{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Kill Server Ports",
      "type": "shell",
      "command": "$ports = 5187,7183; $pids = Get-NetTCPConnection -ErrorAction SilentlyContinue | Where-Object { $_.State -eq 'Listen' -and $_.LocalPort -in $ports } | Select-Object -ExpandProperty OwningProcess -Unique; if ($pids) { $pids | ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue } }",
      "problemMatcher": []
    },
    {
      "label": "Run Server",
      "type": "shell",
      "command": "$ports = 5187,7183; $pids = Get-NetTCPConnection -ErrorAction SilentlyContinue | Where-Object { $_.State -eq 'Listen' -and $_.LocalPort -in $ports } | Select-Object -ExpandProperty OwningProcess -Unique; if ($pids) { $pids | ForEach-Object { Stop-Process -Id $_ -Force -ErrorAction SilentlyContinue } }; dotnet run --project EmployeeManagementSystem2.Server/EmployeeManagementSystem2.Server.csproj",
      "isBackground": true,
      "problemMatcher": {
        "owner": "custom",
        "fileLocation": "absolute",
        "pattern": {
          "regexp": "^(.*)$",
          "message": 1
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Building",
          "endsPattern": "Now listening on"
        }
      }
    },
    {
      "label": "Run Client",
      "type": "shell",
      "command": "npm run dev",
      "options": {
        "cwd": "${workspaceFolder}/employeemanagementsystem2.client"
      },
      "isBackground": true,
      "problemMatcher": {
        "owner": "custom",
        "fileLocation": "absolute",
        "pattern": {
          "regexp": "^(.*)$",
          "message": 1
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "VITE",
          "endsPattern": "ready in"
        }
      }
    },
    {
      "label": "Open Client (Auto Port)",
      "type": "shell",
      "command": "$url = 'https://localhost:56883/'; for ($i = 0; $i -lt 60; $i++) { $listening = Get-NetTCPConnection -State Listen -LocalPort 56883 -ErrorAction SilentlyContinue; if ($listening) { break }; Start-Sleep -Milliseconds 250 }; if (-not (Get-NetTCPConnection -State Listen -LocalPort 56883 -ErrorAction SilentlyContinue)) { throw 'Vite is not listening on https://localhost:56883/. Start the client first.' }; $edgePath = if (Get-Command msedge.exe -ErrorAction SilentlyContinue) { 'msedge.exe' } elseif (Test-Path 'C:/Program Files/Microsoft/Edge/Application/msedge.exe') { 'C:/Program Files/Microsoft/Edge/Application/msedge.exe' } elseif (Test-Path 'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe') { 'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe' } else { $null }; $chromePath = if (Get-Command chrome.exe -ErrorAction SilentlyContinue) { 'chrome.exe' } elseif (Test-Path 'C:/Program Files/Google/Chrome/Application/chrome.exe') { 'C:/Program Files/Google/Chrome/Application/chrome.exe' } elseif (Test-Path 'C:/Program Files (x86)/Google/Chrome/Application/chrome.exe') { 'C:/Program Files (x86)/Google/Chrome/Application/chrome.exe' } else { $null }; Write-Output ('OPENING:' + $url); if ($edgePath) { Start-Process $edgePath $url } elseif ($chromePath) { Start-Process $chromePath $url } else { Start-Process $url }",
      "problemMatcher": []
    },
    {
      "label": "Run Client + Open Browser",
      "dependsOn": [
        "Run Client",
        "Open Client (Auto Port)"
      ],
      "dependsOrder": "sequence",
      "problemMatcher": []
    },
    {
      "label": "Prepare Debug Full Stack",
      "dependsOn": [
        "Kill Server Ports",
        "Run Client + Open Browser"
      ],
      "dependsOrder": "sequence",
      "problemMatcher": []
    },
    {
      "label": "Run Client + Server",
      "dependsOn": [
        "Run Client + Open Browser",
        "Run Server"
      ],
      "dependsOrder": "sequence",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
